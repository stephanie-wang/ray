# An unique identifier for the head node and workers of this cluster.
cluster_name: osdi

# The minimum number of workers nodes to launch in addition to the head
# node. This number should be >= 0.
min_workers: 2

# The maximum number of workers nodes to launch in addition to the head
# node. This takes precedence over min_workers.
max_workers: 2

# The initial number of worker nodes to launch in addition to the head
# node. When the cluster is first brought up (or when it is refreshed with a
# subsequent `ray up`) this number of nodes will be started.
initial_workers: 2

# Whether or not to autoscale aggressively. If this is enabled, if at any point
#   we would start more workers, we start at least enough to bring us to
#   initial_workers.
autoscaling_mode: default

# The autoscaler will scale up the cluster to this target fraction of resource
# usage. For example, if a cluster of 10 nodes is 100% busy and
# target_utilization is 0.8, it would resize the cluster to 13. This fraction
# can be decreased to increase the aggressiveness of upscaling.
# This value must be less than 1.0 for scaling to happen.
target_utilization_fraction: 0.8

# If a node is idle for this many minutes, it will be removed.
idle_timeout_minutes: 5

# Cloud-provider specific configuration.
provider:
    type: aws
    region: us-west-2
    # Availability zone(s), comma-separated, that nodes may be launched in.
    # Nodes are currently spread between zones by a round-robin approach,
    # however this implementation detail should not be relied upon.
    availability_zone: us-west-2a,us-west-2b
    cache_stopped_nodes: false

# How Ray will authenticate with newly launched nodes.
auth:
    ssh_user: ubuntu
# By default Ray creates a new private keypair, but you can also use your own.
# If you do so, make sure to also set "KeyName" in the head and worker node
# configurations below.
#    ssh_private_key: /path/to/your/key.pem

# Provider-specific config for the head node, e.g. instance type. By default
# Ray will auto-configure unspecified fields such as SubnetId and KeyName.
# For more documentation on available fields, see:
# http://boto3.readthedocs.io/en/latest/reference/services/ec2.html#EC2.ServiceResource.create_instances
head_node:
    InstanceType: m4.16xlarge
    ImageId: ami-0764eacdf56f1b2bc

    # Additional options in the boto docs.

# Provider-specific config for worker nodes, e.g. instance type. By default
# Ray will auto-configure unspecified fields such as SubnetId and KeyName.
# For more documentation on available fields, see:
# http://boto3.readthedocs.io/en/latest/reference/services/ec2.html#EC2.ServiceResource.create_instances
worker_nodes:
    InstanceType: m4.16xlarge
    ImageId: ami-0764eacdf56f1b2bc

# Files or directories to copy to the head and worker nodes. The format is a
# dictionary from REMOTE_PATH: LOCAL_PATH, e.g.
file_mounts:
    /home/ubuntu/ray: /home/swang/ray

# List of commands that will be run before `setup_commands`. If docker is
# enabled, these commands will run outside the container and before docker
# is setup.
initialization_commands: []

# List of shell commands to run to set up nodes.
setup_commands:
    - echo set-window-option -g mode-mouse on > ~/.tmux.conf
    - echo 'termcapinfo xterm* ti@:te@' >> ~/.screenrc
    - '[ -z "$RAY_HEAD_IP" ] && echo "On driver node" || echo "On worker node"'
    - echo 'export RAY_GCS_SERVICE_ENABLED=false' >> ~/.bashrc
    # XXX: Uncomment below to build ray from source.
    - cd ray/python; [ -z "$RAY_HEAD_IP" ] && source activate tensorflow_p36 && python setup.py develop || true
    # XXX: Uncomment below to install ray 0.7.7.
    #- source activate tensorflow_p36 && pip install -U ray==0.7.7

# Custom commands that will be run on the head node after common setup.
head_setup_commands:
    - pip install boto3==1.4.8  # 1.4.8 adds InstanceMarketOptions
    - source activate tensorflow_p36 && pip install boto3==1.4.8  # 1.4.8 adds InstanceMarketOptions

# Custom commands that will be run on worker nodes after common setup.
worker_setup_commands: []

# Command to start ray on the head node. You don't need to change this.
head_start_ray_commands:
    - source activate tensorflow_p36 && ray stop
    - source activate tensorflow_p36 && RAY_GCS_SERVICE_ENABLED=false ray start --head --redis-port=6379 --object-manager-port=8076 --autoscaling-config=~/ray_bootstrap_config.yaml --num-cpus=0 --internal-config="{\"record_ref_creation_sites\":0, \"initial_reconstruction_timeout_milliseconds\":100, \"num_heartbeats_timeout\":10, \"lineage_pinning_enabled\":1, \"free_objects_period_milliseconds\":-1, \"object_manager_repeated_push_delay_ms\":1000, \"task_retry_delay_ms\":1000, \"max_direct_call_object_size\":10737418240, \"max_grpc_message_size\":-1}"
    # - source activate tensorflow_p36 && ray start --head --redis-port=6379 --object-manager-port=8076 --autoscaling-config=~/ray_bootstrap_config.yaml --num-cpus=0 --internal-config="{\"record_ref_creation_sites\":0, \"free_objects_period_milliseconds\":-1, \"object_store_full_max_retries\":0, \"object_pinning_enabled\":1, \"distributed_ref_counting_enabled\":1}"
    # - ray start --head --redis-port=6379 --object-manager-port=8076 --autoscaling-config=~/ray_bootstrap_config.yaml --num-cpus=0 --internal-config="{\"record_ref_creation_sites\":0, \"max_direct_call_object_size\":10737418240}"
    # - ray start --head --redis-port=6379 --object-manager-port=8076 --autoscaling-config=~/ray_bootstrap_config.yaml --num-cpus=0 --internal-config="{\"record_ref_creation_sites\":0, \"centralized_owner\":1}"

# Command to start ray on worker nodes. You don't need to change this.
worker_start_ray_commands:
    - source activate tensorflow_p36 && ray stop
    - source activate tensorflow_p36 && RAY_GCS_SERVICE_ENABLED=false ray start --address=$RAY_HEAD_IP:6379 --object-manager-port=8076 --internal-config="{\"record_ref_creation_sites\":0, \"initial_reconstruction_timeout_milliseconds\":100, \"num_heartbeats_timeout\":10, \"lineage_pinning_enabled\":1, \"free_objects_period_milliseconds\":-1, \"object_manager_repeated_push_delay_ms\":1000, \"task_retry_delay_ms\":1000, \"max_direct_call_object_size\":10737418240, \"max_grpc_message_size\":-1}"
    # - source activate tensorflow_p36 && ray start --address=$RAY_HEAD_IP:6379 --object-manager-port=8076 --internal-config="{\"record_ref_creation_sites\":0, \"free_objects_period_milliseconds\":-1, \"object_store_full_max_retries\":0, \"object_pinning_enabled\":1, \"distributed_ref_counting_enabled\":1}"
    # - ray start --address=$RAY_HEAD_IP:6379 --object-manager-port=8076 --internal-config="{\"record_ref_creation_sites\":0, \"max_direct_call_object_size\":10737418240}"
    # - ray start --address=$RAY_HEAD_IP:6379 --object-manager-port=8076 --internal-config="{\"record_ref_creation_sites\":0, \"centralized_owner\":1}"
