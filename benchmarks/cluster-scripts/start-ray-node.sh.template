#!/bin/bash


export PATH=/home/ubuntu/anaconda3/bin/:$PATH

ray start --huge-pages --plasma-directory /mnt/hugepages --redis-address={HEAD}:6379 --resources='{"Node'$(($PSSH_NODENUM + 1))'": 1000}' --num-cpus 100 --use-raylet --use-task-shard --num-redis-shards {NUM_REDIS_SHARDS} --gcs-delay-ms {GCS_DELAY_MS}
